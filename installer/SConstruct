env = Environment()

wml1 = env.File('latemp-main.wml', 'wml/latemp')

install_prefix = '/home/shlomi/apps/latemp/'
wml_install_path = install_prefix + 'lib/wml/include'
env.Install(wml_install_path, wml1)
env.Alias('install', wml_install_path)

