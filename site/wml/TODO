* Fill the content of the pages.

* Set the svn:keywords on the pages.

* Add the Freecell Solver site example once the read-only svn://
source becomes available.

* Add more alternate stylesheets (from CSS Zen Garden, OSWD, etc.)

